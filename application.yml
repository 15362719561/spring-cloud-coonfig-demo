spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/15362719561/config-server-demo  # 远程git仓库的地址
          username: guoantianming  # 以及相应的账户名
          password: hga19920819  # 和密码
          basedir: E:\Java_IDEA\config\basedir  # 可以使用这个配置项来指定本地git仓库的路径
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8974
